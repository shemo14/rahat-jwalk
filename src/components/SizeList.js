import React, { Component } from 'react';import { View, Text, Image  } from 'react-native';import { Button, Icon, Container, Header, Right, Body, Content, Left, List, ListItem, CheckBox } from 'native-base';import axios from "axios/index";import CONST from "../consts";import { DoubleBounce } from 'react-native-loader';class SizeList extends Component{	constructor(props){		super(props);		this.state = {			checked: false,		}	}	setInstallation(){		if(this.state.checked){			this.setState({ checked: false });		}else{			this.setState({ checked: true });		}	}	render(){		const { size } = this.props		return(			<ListItem onPress={() => this.setInstallation()} noBorder style={{ flex: 1, marginLeft: 0, backgroundColor: '#f7f7f9', borderColor: '#f1f1f2', borderWidth: 1, borderRadius: 5, margin: 5, height: 40 }}>				<Left style={{ flex: 1 }}>					<Icon type={'FontAwesome'} name={'database'} style={{ color: '#478947', fontSize: 20, marginRight: 15, marginLeft: 10 }}/>					<Text style={{ color: '#69696a', fontSize: 16, marginLeft: 4, marginRight: 4 }}>{ size.title }</Text>				</Left>				<Right style={{ flex: 1 }}>					<CheckBox style={{ borderRadius: 3, paddingRight: 1 }} checked={this.state.checked} onPress={() => this.setInstallation()} color="#437c1a"/>				</Right>			</ListItem>		)	}}export default SizeList;